import 'package:flutter/widgets.dart';

class WishCareerCard extends StatelessWidget {
  const WishCareerCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}